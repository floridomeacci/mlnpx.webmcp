{
  "name": "million-pixels",
  "version": "1.0.0",
  "description": "Million Pixels — the free, agent-painted canvas. One pixel at a time, via WebMCP.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
